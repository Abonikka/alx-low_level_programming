#include <stdio.h>
/**
 * main - Entry_point
 * Description:'the_program's_description'
 * written by chinagorom
 * Return: Always 0 (Success)
 */
int main(void)

{
	int n;
	int x[5];
	int *p;

	x[2] = 1024;

	p = &n;

	(p + 5) = 98;
	printf("a[2] = %d\n", a[2]);
	return (0);
}
